// double triangle_area(double base, height)    // height has no type
// double product;
// {
// product = base * height;                     // different types being multiplied!!
// return product / 2;
// }

#include <stdio.h>
#include <stdbool.h>

// int check(int, int, int);

// int main1()
// {
//     int value1, value2, range;
//     printf("What value do x and y have, what is n?");
//     scanf("%d %d %d", &value1, &value2, &range);
//     printf("%d\n", check(value1, value2, range));
// }

// int check(int x, int y, int n)
// {
//     bool first = x <= n - 1;
//     bool second = y <= n - 1;
//     return first == second;
// }



/*
Hint: The classic algorithm for computing the GCD, known as Euclid’s algorithm, goes as
follows: Let m and n be variables containing the two numbers. If n is 0, then stop: m contains
the GCD. Otherwise, compute the remainder when m is divided by n. Copy n into m
and copy the remainder into n. Then repeat the process, starting with testing whether n is 0.
*/

gcd(float, float) {

}

int main() {

}